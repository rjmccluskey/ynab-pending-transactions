{
  "name": "ynab-pending-transactions",
  "version": "1.0.0",
  "description": "Import pending transactions to YNAB (You Need a Budget)",
  "main": "index.js",
  "author": "Richard McCluskey",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node -r dotenv/config -e 'require(\"./src/\").main()'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "ts-node": "ts-node",
    "debug": "npm run build && node -r dotenv/config --nolazy --inspect-brk=9229 -e 'require(\"./dist/\").main()'"
  },
  "dependencies": {
    "@infosimples/node_two_captcha": "^1.0.2",
    "puppeteer": "^2.0.0",
    "puppeteer-extra": "^3.1.9",
    "puppeteer-extra-plugin-stealth": "^2.4.9",
    "ynab": "^1.17.0"
  },
  "devDependencies": {
    "@types/node": "^13.1.8",
    "@types/puppeteer": "^2.0.0",
    "dotenv": "^8.2.0",
    "ts-node": "^8.6.2",
    "typescript": "^3.7.5"
  }
}
